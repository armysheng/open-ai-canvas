# Initial ACME-only host, before a certificate is available.
server {
    listen 80;
    server_name movie.iaigc.fun;
    location ^~ /.well-known/acme-challenge/ {
        root /var/www/letsencrypt;
    }
    location / {
        return 503;
    }
}
